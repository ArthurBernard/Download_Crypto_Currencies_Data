version: 2

formats:
  - htmlzip

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc

submodules:
  include: []

sphinx:
  builder: html
  configuration: doc/source/conf.py
  fail_on_warning: false
