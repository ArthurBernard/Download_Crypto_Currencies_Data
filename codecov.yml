# Coverage is tracked but never blocks a merge — especially this v3 rewrite PR,
# whose new surface (FastAPI endpoints, network-only paths, UI glue) is exercised
# by API/E2E rather than line-covering unit tests.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment: false
